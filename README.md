# French-Fluency-Tracker
A small, dependency-free web app for the 60-day French conversational fluency plan (relationship/family focus). No build step, no framework, no backend. Pronunciation audio uses your browser's built-in text-to-speech (Web Speech API) — works fully offline once the page is loaded,
